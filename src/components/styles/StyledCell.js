import styled from "styled-components";

export const StyledCell = styled.div`
  width: auto;
  background: rgba(${props => props.color}, 0.8);
`;

//read about styled components because I don't know anything about that
